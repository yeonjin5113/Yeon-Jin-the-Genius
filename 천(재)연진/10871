#include <iostream>
#include <vector>

using namespace std;

int main()
{
    int N, X, input;   //N : 정수, X : 정수 
    vector<int> vec;
    
    cin >> N >> X;
    
    for (int i=0; i<N; i++){
        cin >>  input;
        vec.push_back(input);
    }

    for (int i=0; i<N; i++){
        if (vec[i] < X){
            cout << vec[i] << " ";
        }
    }
    
}
